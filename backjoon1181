using System;
using System.Linq;
using System.Runtime.CompilerServices;

public class Backjoon1181
{
    public static void Main(string[] args)
    {
        string input = Console.ReadLine();
        int count = int.Parse(input);
        string[] word = new string[count];

        for (int i = 0; i < count; i++)
        {
            word[i] = Console.ReadLine();
        }

        // 중복제거
        word = word.Distinct().ToArray();
        Array.Sort(word, (x, y) => //람다식
        {
            int lengcom = x.Length.CompareTo(y.Length);// 길이비교
            if (lengcom == 0) // 길이가 같으면
                return string.Compare(x, y); // 사전순 비교
            else// 길이가 다르면
                return lengcom; // 길이비교 x가 길면 양수, y가 길면 음수
        });

        // 3. 결과 출력
        foreach (string uniqueWord in word)
        {
            Console.WriteLine(uniqueWord);
        }
    }
}
